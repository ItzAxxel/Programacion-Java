//Programa hecho por Axel y Siru

import java.util.Random;
import java.util.Scanner;

public class Minijuegos {

	static Scanner teclado = new Scanner(System.in);

	public static void main(String[] args) {

		/*
		 * Adivina el numero y Combate
		 * 
		 */

		menu(false, 0);

	}

	public static int menu(boolean salir, int opcion) {

		while (!salir) {
			System.out.println(" Elige una opción: ");
			System.out.println(" 1.- Jugar");
			System.out.println(" 2.- Información");
			System.out.println(" 3.- Mostrar puntuaciones");
			System.out.println(" 4.- Autores");
			System.out.println(" 5.- Versión");
			System.out.println(" 6.- Salir");
			opcion = teclado.nextInt();

			if (opcion == 1) {
				elegirJuego();
			}

			if (opcion == 2) {
				informacion();
				menu(salir, opcion);
			}

			if (opcion == 3) {
				mostrarPuntuaciones();
				menu(salir, opcion);
			}

			if (opcion == 4) {
				System.out.println("Axel y Siru");
				menu(salir, opcion);
			}

			if (opcion == 5) {
				System.out.println("1.0.1");
				menu(salir, opcion);
			}

			if (opcion == 6) {
				salir = true;
			}
		}

		return opcion;
	}

	public static int elegirJuego() {
		int opcionJuego = 0;

		System.out.println(" Disponemos de dos juegos actualmente, ¿Cual quieres jugar?");
		System.out.println(" 1.- Adivina el número ");
		System.out.println(" 2.- Combate ");
		System.out.println(" 3.- Volver al menú principal");
		opcionJuego = teclado.nextInt();
		if (opcionJuego == 1) {
			modoDeJuego(opcionJuego);
			adivinaElNumeroIA(opcionJuego, opcionJuego, opcionJuego, opcionJuego, opcionJuego);
		}
		if (opcionJuego == 2) {
			modoDeJuego(opcionJuego);

		}
		if (opcionJuego == 3) {
			menu(false, opcionJuego);
		}
		return opcionJuego;

	}

	public static void informacion() {
		System.out.println("------------------------------------ Adivina el número ------------------------------");
		System.out.println("En este minijuego, deberás adivinar 3 veces el número aleatorio para ganar la partida");
		System.out.println("---------------------------------------------------------------------------------------");
	}

	public static void mostrarPuntuaciones() {

	}

	public static int modoDeJuego(int elecionModoDeJuego) {
		System.out.println(" Ahora debes elegir el modo de juego: ");
		System.out.println(" 1.- Jugador contra jugador ");
		System.out.println(" 2.- Jugador contra IA");
		System.out.println(" 3.- Volver al menú anterior");
		elecionModoDeJuego = teclado.nextInt();
		if (elecionModoDeJuego == 1) {
			adivinaElNumeroJugadorContraJugador();

		}
		if (elecionModoDeJuego == 2) {
			adivinaElNumeroIA(elecionModoDeJuego, elecionModoDeJuego, elecionModoDeJuego, elecionModoDeJuego,
					elecionModoDeJuego);
		}
		if (elecionModoDeJuego == 3) {
			menu(false, elecionModoDeJuego);
		}

		return elecionModoDeJuego;
	}

	public static void adivinaElNumeroJugadorContraJugador() {

	}

	public static void adivinaElNumeroIA(int numeroUsuario, int numeroAleatorio, int IA, int puntuacionUsuario,
			int puntuacionIA) {
		int contadorRonda = 0;
		Random rand = new Random();
		System.out.println(
				"Este juego es al mejor de tres. Cuando uno de los dos jugadores gane 3 veces, será el vencedor.");

		numeroAleatorio = rand.nextInt(10) + 1;
		while (numeroUsuario != numeroAleatorio || IA != numeroAleatorio) {
			contadorRonda++;

			numeroUsuario = 0;
			IA = 0;
			numeroAleatorio = rand.nextInt(10) + 1;
			System.out.println(numeroAleatorio + " numero aleatorio probar");

			System.out.println("Estas en la ronda: " + contadorRonda);
			System.out.println("Elige un número del 1 al 10: ");
			numeroUsuario = teclado.nextInt();

			System.out.println();
			System.out.println("La IA está eligiendo número...");
			elecionNumeroIA(IA);
			System.out.println("Ha elegido el número " + IA);

			puntuacionUsuarioAdivinaElNumero(numeroUsuario, numeroAleatorio);
			puntuacionIAAdivinaElNumero(IA, numeroAleatorio);
			ganaUsuario(puntuacionUsuario);
			ganaIA(puntuacionIA);

		}
	}

	public static int elecionNumeroIA(int IA) {
		Random rand = new Random();
		try {
			// Ponemos a "Dormir" el programa durante los ms que queremos
			Thread.sleep(2000);
			IA = rand.nextInt(10) + 1;
		} catch (Exception e) {
			System.out.println(e);
		}
		return IA;
	}

	public static int puntuacionUsuarioAdivinaElNumero(int numeroUsuario, int numeroAleatorio) {
		int puntuacionUsuario = 0;
		if (numeroUsuario == numeroAleatorio) {
			System.out.println("¡Has ganado la ronda!");
			puntuacionUsuario++;
		}

		return puntuacionUsuario;
	}

	public static int puntuacionIAAdivinaElNumero(int IA, int numeroAleatorio) {
		int puntuacionIA = 0;
		if (IA == numeroAleatorio) {
			System.out.println("¡La IA ha ganado la ronda!");
			puntuacionIA++;
		}

		return puntuacionIA;
	}

	public static int ganaUsuario(int puntuacionUsuario) {
		if (puntuacionUsuario == 3) {
			System.out.println("¡Felicidades has ganado la partida!");
		}
		return puntuacionUsuario;

	}

	public static int ganaIA(int puntuacionIA) {
		if (puntuacionIA == 3) {
			System.out.println("¡La IA ha ganado la partida!");
		}
		return puntuacionIA;
	}

}
